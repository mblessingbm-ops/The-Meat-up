// hooks/useHR.ts
// Stub — HR module removed from The Meat Up. File kept to avoid import errors.
export function useHR() {
  return { employees: [], leaveRequests: [], loading: false, error: null }
}
